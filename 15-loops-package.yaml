- name: Install packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: Installing package
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    loop:
      - nginx
      - zip
      - mysql

