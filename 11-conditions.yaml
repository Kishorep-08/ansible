- name: Conditions
  hosts: local
  connection: local
  tasks:
  - name: checks whether given number is less than 10 or not
    ansible.builtin.debug:
      msg: "Given number {{ NUMBER }} is less than 10"
    when: NUMBER | int < 10